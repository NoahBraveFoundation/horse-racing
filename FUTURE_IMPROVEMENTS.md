# Future Improvements

- Explore consolidating carts so each user has a single long-lived cart, with historical purchases captured separately, to further simplify ticket management and avoid duplicate self-assigned tickets when switching seasons.
