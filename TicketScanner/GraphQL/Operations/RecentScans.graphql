query RecentScans($limit: Int!) {
  recentScans(limit: $limit) {
    id
    scanTimestamp
    scanLocation
    ticket {
      id
      attendeeFirst
      attendeeLast
      seatAssignment
    }
    scanner {
      firstName
      lastName
    }
  }
}

